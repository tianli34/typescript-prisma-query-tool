
我希望能够在Windows  PowerShell 中使用项目的脚本功能，大致方案是：

1. 新建一个 CLI 入口文件（如 `src/cli.ts`），使用 Node.js 的 [`tsx`](https://github.com/privatenumber/tsx) 运行
2. 引入 [`prismaParser.ts`](src/lib/prismaParser.ts) 和 [`queryEngine.ts`](src/lib/queryEngine.ts) 的核心逻辑
3. 从固定路径（如E:\typescript-prisma-query-tool\src\lib\sampleSchema.ts）读取 `.prisma` schema 文件作为输入
4. 通过 `process.argv` 解析命令行参数，调用 [`dispatch()`](src/lib/queryEngine.ts:335) 函数
5. 将 `QueryResult` 格式化输出到终端

如果这是一个好方案，请你设计和实现这个 CLI 功能。
